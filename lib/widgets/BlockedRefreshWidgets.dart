import 'package:flutter/material.dart';

class BlockedRefreshWidgets extends StatelessWidget {
  const BlockedRefreshWidgets({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
